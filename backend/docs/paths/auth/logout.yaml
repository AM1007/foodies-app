post:
  tags:
    - Auth
  summary: Вихід із системи
  description: Інвалідація токену користувача
  operationId: logoutUser
  security:
    - bearerAuth: []
  responses:
    '204':
      description: Успішний вихід із системи
    '401':
      description: Неавторизований доступ
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/Error.yaml'
